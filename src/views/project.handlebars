<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="theme-color" content="#B5A691" />

  <meta name="description" content="{{ project.Description }}">

  <title>{{ project.Name }}</title>

  <link rel="preload" href="/styles.css" as="style">
  <link rel="preload" href="/abel.ttf" as="font" type="font/ttf" crossorigin>

  <link rel="stylesheet" href="/styles.css">

</head>


<body>
  {{> topnav }}
  <div class="subheader projectHeader" style="background-image: url('/file/a/{{banner.FileName}}_MediumHigh.jpg');">
    <h1 class="projectBannerText noselect"
      onclick="document.getElementById('main').scrollIntoView({behavior: 'smooth'});">ᐯ</h1>
  </div>
  <main class="content project" id="main">
    <div class="projectInfo">
      <h1>{{ project.Name }}</h1>
      {{#if project.Description }}
      {{ project.Description }}
      {{^}}
      No description provided.
      {{/if}}
      <div class="tags">
        {{#each tags }}
        <a class="tag noselect" href="/browse?tag={{this.TagID}}">{{ this.Name }}</a>
        {{/each}}
      </div>
    </div>

    {{#if content }}
    <div class="extraProjectContent">
      {{#each content }}
      {{ this.Description }}
      <picture class="galleryContent">
        <source srcset="/file/a/{{ this.FileName }}_Default.jpg 3840w,
                  /file/a/{{ this.FileName }}_High.jpg 2880w,
                  /file/a/{{ this.FileName }}_MediumHigh.jpg 1440w,
                  /file/a/{{ this.FileName }}_Medium.jpg 1920w,
                  /file/a/{{ this.FileName }}_Low.jpg 1080w">
        <img decoding="async" loading="lazy" id="{{this.ID}}" src="/file/a/{{ this.FileName }}_Low.jpg"
          alt="{{ this.Description }}">
      </picture>
      {{/each}}
    </div>
    {{/if}}

  </main>
  {{> footer }}
</body>

</html>